version: "3.8"

networks:
        mtbAI:

services:
         python:
                build:
                        dockerfile: Dockerfile
                container_name: mtb_AI
                restart: "no"
                ports:
                        - "51001:22"
                volumes:
                          - /nextcloud/data:/home/dev/data/Clinical_Trials_Snapshot # symlink to mount /nextcloud/data to /data on container
